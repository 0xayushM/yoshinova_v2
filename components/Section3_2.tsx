/**
 * MPS Deployment.
 *
 * Replaced the 3×2 plate grid with a pinned horizontal rail — see
 * components/ZoneRail.tsx. All six zones and the original copy are intact;
 * each card now also carries typical system size and the headline result,
 * which is what the extra room on a rail is for.
 */
export { default } from "./ZoneRail";
