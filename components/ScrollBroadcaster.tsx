/**
 * REMOVED in v2 — never mounted on the live site; the 'drei-scroll' event it dispatched was dead code
 *
 * Kept as an empty module so no stale import can silently resurrect it.
 * Safe to delete this file.
 */
export {};
