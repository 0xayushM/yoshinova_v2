/**
 * REMOVED — merged into components/Navbar.tsx.
 *
 * Having a second navbar for non-home routes is what let tone drift: two
 * components, two sets of colour rules, and "Request Audit" invisible on
 * whichever one hadn't been updated. Navbar now derives its own tone.
 * Safe to delete this file.
 */
export {};
