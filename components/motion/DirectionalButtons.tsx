/**
 * REMOVED — direction-aware button fill.
 *
 * Tried entering the fill from whichever edge the cursor crossed. The fixed
 * bottom-up sweep reads better, so the button fill lives entirely in CSS
 * again (see `.btn::before` in app/globals.css). Safe to delete this file.
 */
export {};
