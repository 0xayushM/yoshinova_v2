/**
 * REMOVED in v2 — framer-motion opacity fade. Replaced by CurtainTransitionProvider in components/Curtain.tsx
 *
 * Kept as an empty module so no stale import can silently resurrect it.
 * Safe to delete this file.
 */
export {};
